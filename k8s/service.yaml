apiVersion: v1
kind: Service
metadata:
  name: sentinel-poc
  namespace: sentinel-poc
  labels:
    app: sentinel-poc
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: sentinel-poc
