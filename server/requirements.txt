# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP client
requests==2.31.0

# GDAL Python bindings (for mock client)
# Note: GDAL is installed at system level in Docker, 
# Python bindings come with it
# GDAL>=3.6.0

# Utilities
python-multipart==0.0.6
aiofiles==23.2.1

# Geospatial
numpy>=1.24.0
rasterio>=1.3.0
pystac-client>=0.7.0
scipy>=1.10.0

# Computer Vision with DNN Super-Resolution
# opencv-contrib-python includes dnn_superres module
opencv-contrib-python>=4.8.0

# PyTorch for Real-ESRGAN and SwinIR
# Use CPU version by default - for GPU use torch with CUDA
torch>=2.0.0
torchvision>=0.15.0

